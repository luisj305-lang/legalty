# Mercado Pago — sandbox credentials (server-side only, never commit real values)
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=
MP_WEBHOOK_SECRET=

# Contact form delivery endpoint (receives { name, email, message })
CONTACT_ENDPOINT=
